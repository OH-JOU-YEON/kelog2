<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.persistence.RoutineMapper">

   <insert id="insert">
        INSERT INTO routine (content, eventDate, email)
        VALUES (#{content}, #{eventDate}, #{email})
    </insert>
    
    <!-- 일정 수정 -->
    <update id="update">
        UPDATE routine
        SET content = #{content}, eventDate = #{eventDate}
        WHERE routineNo = #{routineNo} AND email = #{email}
    </update>

    <!-- 일정 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM routine WHERE routineNo = #{routineNo}
    </delete>

    <!-- 특정 이메일에 해당하는 일정 목록 조회 -->
    <select id="getEventsByEmail" parameterType="String">
        SELECT routineNo, content, eventDate email
        FROM routine
        WHERE email = #{email}
    </select>

</mapper>
